---
title: Reference
sidebar_position: 3
description: abc axxxccddeasd
payloadMatter: d32d32d3d23

---

import * as Search from './api/search.md'
import React from 'react'

export default function () {
    const pages = [Search]

    console.log(pages)

    return <>
        {pages.map((module, index) => {
            const Page = module.default

            return <React.Fragment key={index}>
                <h2>{module.metadata.title}</h2>
                <div style={{
                    border: '1px solid #eaecef',
                    padding: '20px',
                }}>
                    <Page key={index}/>
                </div>
            </React.Fragment>
        })}
    </>
}
